services:
  opspilot:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - PORT=8000
      - HOST=0.0.0.0
      - INTERNALOPS_DB=/data/internalops.db
    volumes:
      - opspilot-data:/data

volumes:
  opspilot-data:
